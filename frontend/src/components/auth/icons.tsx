/**
 * Auth-flow icons, re-exported from `@animateicons/react/lucide` (hover-animated,
 * `prefers-reduced-motion` aware). They render `<div><svg/></div>`, take a `size`
 * prop instead of an `h-* w-*` class, and tint from the surrounding `text-*`.
 */
export {
  ArrowRightIcon,
  ArrowLeftIcon,
  EyeIcon,
  EyeOffIcon,
} from "@animateicons/react/lucide";
